#!/bin/bash

# Check if user pijuice exists
if ! id -u pijuice > /dev/null 2>&1; then
	echo "User pijuice does not exist"
	echo "Is pijuice-base (>= 1.4) installed?"
	exit 1
fi

# pijuice_tray runs in the desktop session via /etc/xdg/autostart; not started from
# here (root, no display: it would only fail with its output discarded).

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0
