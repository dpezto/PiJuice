#!/bin/sh

# Kill running tray instances (they run as the desktop user).
pkill -f pijuice_tray.py || true
set -e

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0
