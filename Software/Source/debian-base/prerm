#!/bin/sh
set -e
# Stop the service before its files go away (was generated by dh_systemd_start).
if [ -d /run/systemd/system ] && [ "$1" = remove ]; then
	systemctl stop pijuice.service
	systemctl disable pijuice.service
fi
exit 0
